// localStorage key for the display name ( same string we send as `author` ) on POST request
export const AUTHOR_STORAGE_KEY = 'chat-author-name'

// Shown when there’s no saved name yet 
export const DEFAULT_AUTHOR_NAME = 'Paul Ekunola'
